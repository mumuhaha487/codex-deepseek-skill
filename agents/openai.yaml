interface:
  display_name: "deepseek"
  short_description: "配置继承父 Provider 的 DeepSeek 子 Agent、思考强度与识图能力"
  default_prompt: "使用 $deepseek 配置可写 CustomAgent，并让主 Agent 通过隔离 Git worktree、检查点、功能验收、回退和清理来管理子智能体直接修改代码。"
